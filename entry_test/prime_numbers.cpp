#include <iostream>
#include <cstdlib>
#include <cmath>
using namespace std;

int initial_size=1;
int top_limit=10000;
int limiter=int(sqrt(top_limit));

int main() 
{
    vector<vector<int>> a;
				int i=0;
    while (i < limiter)
				{
								
				}
}
